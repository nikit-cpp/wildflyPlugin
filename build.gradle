apply plugin: 'groovy'
apply plugin: 'maven-publish'

dependencies {
    compile gradleApi()
    compile localGroovy()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId 'com.github.nikit.cpp'
            artifactId 'wildflyPlugin'
            version '0.1'
            from components.java
        }
    }
}

publishing {
    repositories { maven { url "http://dl.bintray.com/nikit007/maven" } }
}